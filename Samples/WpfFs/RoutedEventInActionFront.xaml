<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:model="clr-namespace:WpfFs.Models;assembly=WpfFs"
             x:Class="WpfFs.UI.RoutedEventInActionFront"
             >
  <UserControl.DataContext>
    <model:RoutedEventInActionModel />
  </UserControl.DataContext>
  <UserControl.Resources>
    <Style TargetType="Border">
      <Setter Property="Padding" Value="1cm" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Margin" Value=".05cm" />
      <Setter Property="Padding" Value=".1cm" />
    </Style>
  </UserControl.Resources>
  <Grid>
    <Border Background="Gray">
      <Border Background="White">
        <Border Background="Gray">
          <Border Background="White">
            <Border Background="Gray">
              <StackPanel>
                <Button Click="ShowPopup">Click to show a modal dialog with Routed Event</Button>
                <Button Command="{Binding ShowPopupCommand}">Click to show a modal dialog with Command</Button>
              </StackPanel>
            </Border>
          </Border>
        </Border>
      </Border>
    </Border>
  </Grid> 
</UserControl>